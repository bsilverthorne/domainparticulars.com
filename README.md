# domainparticulars.com

Single-page static site for **Domain Particulars, LLC** (Longview, Texas),
served by GitHub Pages from `main` with the custom domain in `CNAME`.

- `main` — live site.
- `site-v2` — **staged rebuild (2026-07-15)**: Field Kit identity
  (bone/graphite/safety-orange, registration mark), two-track copy per
  `dp-dir/03_strategy/messaging-2026-07.md`. Merges to `main` only on Bryan's
  explicit go.
- `site-v1` — rejected 2026-07-15 redesign (seal/parchment identity); kept as
  history, do not build on it.

Identity source of truth: `dp-dir/07_assets/identity-guide.md` + `logo/`.
No build system — plain HTML/CSS, one Google Fonts request (Roboto Slab).
